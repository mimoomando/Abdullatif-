# تشغيل البوت — الورقةُ المرجعيّة

> **ارجع إلى هذا الملفّ متى شئت.** وهو داخل المجلّد نفسه، فيصلك مع
> كلّ `git pull` ولا يضيع بإغلاق نافذة.

---

## ⭐ الأسهل — نقرتان

انقر نقرًا مزدوجًا على:

```
run.bat
```

وهو يسحب التحديثَ ثمّ يشغّل البوت. ولا تكتب شيئًا.

---

## أو بالأمر — ثلاثةُ أسطر

```
cd C:\Users\mando\Desktop\Abdullatif-
git pull
python -m bot.runner --watch --every 60
```

---

## وعلامةُ أنّه يعمل صحيحًا — هذان السطران

```
PAPER MODE - no orders are ever sent. Recording only.
every 60s - stop with Ctrl+C
```

⇒ `PAPER MODE` يعني **لا أوامرَ تُرسَل إلى المنصّة أبدًا** — تسجيلٌ
فقط، حتّى لو ظهر إعدادٌ ممتاز.

---

## الإيقاف

| ما تريد | ما تفعل |
|---|---|
| إيقافٌ تامّ | `Ctrl+C` في النافذة |
| إيقافُ التنبيه فقط | ضع ملفًّا اسمه `STOP` في هذا المجلّد |
| العودة | احذف ملفّ `STOP` |

⚠️ و`STOP` **يوقف التنبيه لا التسجيل** — فالبوت يبقى يقرأ ويكتب،
لنعرف ما **كان** سيحدث في الساعات التي أوقفتَه فيها.

⭐ واكتب داخل الملفّ سببَ الإيقاف في سطر. فمن أوقفه الخميس وعاد
الاثنين لا يذكر لماذا.

⛔ **وإن كان `STOP` موجودًا نسيتَه، لن يُنبّهك البوت وإن عمل.** فإذا
صمت يومًا كاملًا، أوّلُ ما تفحصه: هل في المجلّد ملفُّ `STOP`؟

---

## السجلّ

```
runs\decisions.jsonl
```

فيه كلُّ قرار — ما دخله، وما رفضه، **وسببَ الرفض**. وهو الملفّ الذي
ترسله آخرَ الأسبوع.

⇒ ولا يضيع شيءٌ بإغلاق النافذة: السجلُّ **يُلحَق ولا يُمحى**. فإن
انقطع الاتّصالُ أو أُغلقت النافذةُ بالخطأ، شغّله ثانيةً بنفس الأمر.

---

## ما يُتوقَّع

**الصمتُ طبيعيّ.** الإعدادُ الحيّ يعطي **إعدادين في الأسبوع** تقريبًا
— فقد يمرّ يومان أو ثلاثة بلا صفقةٍ واحدة. وليس عطلًا.

---

## للقياس فقط — وليس أثناء عمل البوت

```
python -m bot.backtest --rule path --from 2026-08-26 --to 2026-09-18 --higher H1 --detail
```

⛔ **أوقف البوت أوّلًا** (`Ctrl+C`). فالاتّصالُ بالمنصّة واحد،
وبرنامجان يتنازعانه يعطيان `IPC send failed`.
