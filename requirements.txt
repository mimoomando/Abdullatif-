# متطلّبات التشغيل على جهازك (ويندوز)
#
#     pip install -r requirements.txt
#
# ⚠️ النواة كلها **مكتبة قياسية** — لا شيء هنا لازمٌ للاختبارات ولا
#    للتحليل نفسه. هذه للاتصال بالطرفية ولتحويل الصور فقط.

# جسر MetaTrader 5 — **ويندوز فقط**. لا يُثبَّت على لينكس ولا ماك،
# ولهذا يُستورَد داخل دالة لا في رأس الوحدة.
MetaTrader5>=5.0.45

# اختياري: تحويل الشارت من SVG إلى PNG.
# بدونه تُحفظ الشارتات SVG وتُفتح في أي متصفّح — والأسبوع لا يتعطّل.
# cairosvg>=2.7
