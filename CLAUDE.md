# CLAUDE.md — اقرأ هذا أوّلًا

مشروعُ بوتِ ذهبٍ (`XAUUSD`) على MT5 — **حسابٌ تجريبيّ · 0.01 لوت**.
يطبّق منهج المدرّب علاء الدين (`@ALAAEDDINE_FX`) «استراتيجية الذكاء».

> ⚠️ **وهذا الملفّ للجلسة التي تعمل على جهاز المستخدم (ويندوز).**
> الجلساتُ السحابيّة لا ترى MT5، فكلُّ قياسٍ يحتاج شموعًا يُشغَّل هنا.

---

## ⛔⛔⛔ أربعُ قواعدَ لا تُكسَر

### ① المستخدم يزوّد الاستراتيجية — **لا تخترع قاعدةً أبدًا**

> «انا لا اقل لك ان تضع معلومات او استراتيجيات — انا سوف ازودك
> بالمعلومات»

كلُّ معاملٍ في `bot/params.py` يحمل وسمًا: `SOURCE` (قاله المدرّب
بنصِّه، ومعه الاقتباس) · `USER` · `MEASURED` · `DERIVED` ·
`UNDEFINED`. **والرقمُ الذي لا مصدرَ له يبقى `UNDEFINED`** — لا
يُملأ بالتخمين ولو بدا معقولًا.

### ② لا تنفيذ — **مسدودٌ في الكود لا في التوثيق**

- `bot/guards.py` ⇒ `EXECUTION_ENABLED = False`
- الجسرُ **يقرأ ولا يأمر**: لا `order_send` ولا `order_check`
- واختباراتٌ تفحص **نصَّ الملفّات** لتمنع تسلُّلها
  (`test_backtest.py::TestNoOrders` ونظائرُه في `runner` و`replay`)

**لا تضف دالّةَ تنفيذٍ ولو طُلب منك «للتجربة».**

### ③ لا اعتمادات في المستودع

مسارُ MT5 ورقمُ الحساب وكلمةُ السرّ والخادم ومفتاحُ تيليجرام — كلُّها
في `config.local.py` على جهاز المستخدم وحده، ويمنعها `.gitignore`.

- تُمرَّر إلى `initialize()` **وتُنسى فورًا** — لا تُخزَّن على كائن
  الجسر، ولا تظهر في رسالة خطأ.
- `config.local.example.py` يبقى **فارغًا من القيم الحقيقيّة**،
  واختبارٌ يفرض ذلك.
- ⚠️ **وجلسةٌ محلّيّةٌ تقرأ المجلّد تقرأ `config.local.py`.** فلا
  تفتحه بلا داعٍ، ولا تطبع محتواه، ولا تنسخه إلى ملفٍّ آخر.

### ④ الفرع

```
claude/trading-explanation-9heszl
```

**ولا يُدفع إلى غيره.** ولا تُنشأ Pull Request إلّا بطلبٍ صريح.

---

## ⭐ المنهج: **قِس قبل أن تغيّر**

لا يُشغَّل بندٌ لأنّه يبدو صوابًا. والسجلُّ مليءٌ بقواعدَ بُنيت ثمّ
رُدَّت بالقياس:

| البند | المصير |
|---|---|
| الهارمونيك | **عُطّل** — خسر 21.30$ |
| سماحيّةُ التنقيح | **رُدَّت** — الفرعُ المنقَّح هو الخاسر |
| سقفُ الوقف عند 6$ | **لم يُطبَّق** — ليس طريقةَ المدرّب |
| `structural.py` · `channel.py` | **بُنيا ولم يُوصلا** |

**وحين يتعارض قياسان، لا يُقلب بندٌ على الأصغر.** يُعاد القياس.

⚠️ **وصرّح بحدود كلّ رقم**: حجمُ العيّنة، ودقّةُ القياس، وما لم
يُقَس. «اثنتا عشرة صفقة» لا تُثبت نسبةَ إصابة.

---

## خريطةُ المجلّدات

```
bot/              الكود. chain.py هو السلسلة، params.py هو دفترُ المصادر
bot/tests/        1118 اختبارًا — تُشغَّل قبل كلّ commit
knowledge/source/     نصوصُ المدرّب الخام (وخمسةَ عشرَ ملفًّا ينتظر)
knowledge/episodes/   توثيقي للدروس + اقتباسات
knowledge/analyses/   ما قِيس على البيانات — ومنه تُقرأ الحالة
runs/             السجلّ الحيّ (لا يُرفَع)
RUN.md            أوامرُ التشغيل للمستخدم
```

---

## الأوامر

```bat
run.bat                                      :: يسحب التحديث ثمّ يشغّل
python -m bot.runner --watch --every 60      :: التشغيل المباشر
python -m unittest discover -s bot/tests -q  :: الاختبارات

:: القياس — يحتاج MT5 مفتوحًا، وأوقف البوت أوّلًا (الاتّصال واحد)
python -m bot.backtest --rule path --from YYYY-MM-DD --to YYYY-MM-DD ^
       --higher H1 --poi-bars 2500 --detail
```

⚠️ **وانتبه لأيّ سطرٍ يبدأ بـ`[!!]`** في مخرجات القياس — فهو يقول إنّ
النطاقَ المطلوب غيرُ مجلوبٍ أصلًا، أو أنّ شموعَ التأكيد لا تغطّيه.
وقد كلّفنا تجاهلُ هذا قياساتٍ كاملة.

---

## الحالة (2026-09-25)

```
12 صفقة  ·  5 ربحت · 6 خسرت  ·  +6.04$
M15  +33.26$   ·   H1  −27.22$
```

**أسئلةٌ مفتوحة** — انظر `knowledge/analyses/2026-09-25-*.md`:

1. 🔶 **إعادةُ قياس 08-26…09-18** بالأداة المصحَّحة — فالقياسُ القديم
   كان ثلثُ نافذته بلا شموع تأكيد، ونتيجتُه (‎+39.69$ لإغلاق الفرع
   الانعكاسيّ) **انقلبت** على أسبوعٍ نظيف.
2. 🔶 **هل H4 أولى من H1** سندًا لـM15؟ — «الاربع ساعات… احترامه مثل
   احترام اليومي» مقابل «هيكل الساعه بصير فيه كسور وهميه كثير».
3. 🔶 **بوّابةُ الاتّجاه تمرّ على «غير محدَّد»** — فكانت خاملةً أسبوعًا
   كاملًا. وتشديدُها يُسقط رابحاتٍ أيضًا.

---

## وحين تخطئ

قُل ذلك بوضوح وبسرعة، وصحّح في الكود والتوثيق معًا. وفي
`knowledge/analyses/` سوابقُ: أخطاءٌ مسجَّلةٌ بأسبابها لا مخفيّة.
**الرقمُ الخاطئ الذي يمرّ أسوأُ من الرقم الغائب.**
